{
  "name": "@dpacks/repository",
  "version": "0.1.1",
  "description": "dPack's Repository Client Library.",
  "main": "index.js",
  "contributors": [
    "Jared Rice Sr. <jared@dwebs.io>",
    "Distributed Webs Project, LLC. <labs@distributedwebs.org>"
  ],
  "license": "MIT",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "test": "standard && npm run deps && echo \"Error: no tests\" && exit 0"
  },
  "dependencies": {
    "@dwauth/client": "dwauth/client#master",
    "nets": "^3.2.0",
    "xtend": "^4.0.1"
  },
  "devDependencies": {
    "dependency-check": "^2.9.1",
    "standard": "^10.0.3"
  },
  "publishConfig": {
    "access": "public"
  }
}
